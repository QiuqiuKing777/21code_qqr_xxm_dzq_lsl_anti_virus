#'tis for route
#we could place all route in the same file main
#it implements the API with frontend
#mark it when ever you return a value in the overridden func here,\
#check the related function in frontend
#believe in the codes in backend temporarily

#in advance,add an API.txt,marking the related api para and url and return model data
#Handle exception,like overflowing data id